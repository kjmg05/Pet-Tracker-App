import { DefaultTheme } from "react-native-paper";

const theme = {
  ...DefaultTheme,
  colors: {
    ...DefaultTheme.colors,
    dark: "#393E46",
    light: "#EEEEEE",
    darkBlue: "#00ADB5",
    lightBlue: "#AAD8D3",
  },
};

export default theme;